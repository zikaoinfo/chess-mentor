# Angular 22 released ahead of NgRx's matching major: @ngrx/signals@21 declares
# a peer of @angular/core@^21 but is API-compatible with 22. Relax peer
# resolution so both `npm install` and CI's `npm ci` succeed.
legacy-peer-deps=true
